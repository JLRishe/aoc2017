const ramda = require('ramda');
const { __, compose, map, filter } = ramda;
const { probe } = require('aoc-helpers');

const p1 = () => 0;

const p2 = () => 0;

module.exports = {
    solution: {
        type: '',
        ps: [p1, p2]
    }
};